---
import LinkCard from '@components/ui/LinkCard.astro'

export interface Props {
  href: string
  name: string
}

const { href, name } = Astro.props
---

<LinkCard {href} text={Astro.slots.has('default') ? undefined : name} title={`Go to ${name} GitHub repository`}>
  <slot />
</LinkCard>
