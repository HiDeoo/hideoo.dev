---
import { type HTMLAttributes } from 'astro/types'

export interface Props extends HTMLAttributes<'svg'> {
  name: string
}

const { name, ...others } = Astro.props
---

<svg {...others}>
  <use href={`#${name}`}></use>
</svg>
