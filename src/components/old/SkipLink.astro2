<a href="#main">Skip to content</a>

<style>
  a {
    background-color: var(--bg-color);
    border-radius: var(--radius-2);
    box-shadow: 0 0 0 3px var(--color);
    color: var(--color);
    left: -500%;
    outline: none;
    padding: 0.65rem var(--size-4);
    position: absolute;
    text-decoration: none;
    top: var(--size-4);
    z-index: 10;

    &:focus-visible {
      left: var(--size-4);
    }
  }
</style>
